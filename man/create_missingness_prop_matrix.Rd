% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_fun.R
\name{create_missingness_prop_matrix}
\alias{create_missingness_prop_matrix}
\title{Create Missingness Proportion Matrix}
\usage{
create_missingness_prop_matrix(
  data,
  index_col = NULL,
  na_values = c(NA, NaN, Inf, -Inf),
  by_row = FALSE
)
}
\arguments{
\item{data}{Data frame or matrix containing the input data with potential missing values}

\item{index_col}{String name of index column to exclude from analysis (optional)}

\item{na_values}{Vector of values to treat as missing (default: c(NA, NaN, Inf, -Inf))}

\item{by_row}{Logical: if TRUE, compute missingness by row; if FALSE, by column (default: FALSE)}
}
\value{
Matrix where rows are samples and columns are features, with entries as missingness proportions \link{0,1}
}
\description{
Creates a matrix where each entry represents the proportion of missing values
for each feature across samples. This matrix can be used with cluster_on_missing_prop
to identify features with similar missingness patterns.
}
\examples{
# Create sample data with missing values
data <- data.frame(
  sample_id = 1:100,
  feature1 = c(rnorm(80), rep(NA, 20)),
  feature2 = c(rep(NA, 30), rnorm(70)),
  feature3 = rnorm(100)
)

# Create proportion matrix
prop_mat <- create_missingness_prop_matrix(data, index_col = "sample_id")
print(dim(prop_mat))  # Should be 100 x 3
print(head(prop_mat))
}
