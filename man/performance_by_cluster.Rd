% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarizers.R
\name{performance_by_cluster}
\alias{performance_by_cluster}
\title{Calculate performance metrics by cluster and subgroups}
\usage{
performance_by_cluster(
  original_data,
  model,
  dataset,
  clusters,
  index_col = NULL,
  grouping_vars = NULL,
  device = "cpu",
  metrics = c("mse", "mae", "correlation"),
  only_validation = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{original_data}{Original data frame with true values (for grouping variables)}

\item{model}{Trained Python CISS-VAE model object}

\item{dataset}{Python ClusterDataset object used for training (contains validation data)}

\item{clusters}{Vector of cluster assignments}

\item{index_col}{Optional name of index column to align data}

\item{grouping_vars}{Optional vector of column names to group performance by (e.g., c("race", "sex"))}

\item{device}{Device for model inference ("cpu" or "cuda")}

\item{metrics}{Vector of metrics to compute (default: c("mse", "mae", "correlation"))}

\item{only_validation}{Logical; if TRUE, only compute metrics on validation (masked) entries}

\item{verbose}{Logical; if TRUE, prints debug information}
}
\value{
Data frame with performance metrics by cluster and optional subgroups
}
\description{
Calculate performance metrics by cluster and subgroups
}
