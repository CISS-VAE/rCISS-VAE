% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_venv.R
\name{setup_python_env}
\alias{setup_python_env}
\title{Create or re-create the Python virtual environment}
\usage{
setup_python_env(
  envpath,
  python = NULL,
  packages = c("torch", "numpy"),
  install_miniconda = FALSE
)
}
\arguments{
\item{envpath}{Path to create/use the virtualenv.}

\item{python}{Optional path to the Python binary. If \code{NULL}, the function tries:
\enumerate{
\item \code{Sys.which("python3")} and \code{Sys.which("python")}
\item \code{system2("which", "python", stdout = TRUE, stderr = FALSE)}
\item \code{reticulate::py_discover_config()}
}}

\item{packages}{Character vector of PyPI packages to install. Defaults to \code{c("torch","numpy")}.}

\item{install_miniconda}{Logical; if \code{TRUE} and no other Python is found, installs Miniconda via reticulate. Defaults to \code{FALSE}.}
}
\value{
Invisibly returns \code{envpath}.
}
\description{
This helper lives inside your package and can be called by users after
\code{library(yourpkg)}. It will:
\itemize{
\item Look for a Python interpreter in several ways
\item Optionally install Miniconda if none is found
\item Create a venv at \code{envpath} (if missing)
\item Install the requested PyPI packages
}
}
